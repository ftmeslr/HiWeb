import { Stack, styled } from "@mui/material";

export const ProductCardStyled = styled(Stack)(({ theme }) => ({
  width: "100%",
  boxShadow: " 0px 2px 8px 0px rgba(0, 0, 0, 0.1)",
  borderRadius: "10px",
}));
